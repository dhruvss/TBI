name: tbi-tracer

channels:
  - conda-forge

dependencies:
  - python=3.11
  - pip

  # Core scientific Python
  - numpy
  - pandas
  - scipy
  - matplotlib

  # Cheminformatics and molecular preparation
  - rdkit
  - openbabel
  - pdbfixer
  - openmm

  # Docking
  - vina

  # R and analysis packages
  - r-base
  - r-dplyr
  - r-tidyr
  - r-readr
  - r-stringr
  - r-ggplot2
  - r-ggrepel

  # Packages installed through pip
  - pip:
      - meeko
